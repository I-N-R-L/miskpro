{
    "name": "inrl-bot-md",
    "description": "Multi device bot.",
    "keywords": ["wabot, md"],
    "repository": "fixedbotteastingbyinrl/miskpro",
    "stack": "container",
    "env": {
        "SESSION_ID": {
            "description": "Session u received",
            "required": true
        },

        "HEROKU_API_KEY": {
            "description": "Heroku API Key.",
            "required": true
        },

        "HEROKU_APP_NAME": {
            "description": "Heroku APP Name.",
            "required": true  
              }
        },
       
  }
